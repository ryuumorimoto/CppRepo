#include <bits/stdc++.h>
#define rep(i,n) for(int i=0;i<(n);++i)
using namespace std;

int main(){
    int N,M;
    cin >> N >> M;
    long long X[N];
    int C[M];
    long long Y[M];
    rep(i,N){
        cin >> X[i];
    }
    rep(i,M){
        cin >> C[i] >> Y[i];
    }

    


    return 0;
}